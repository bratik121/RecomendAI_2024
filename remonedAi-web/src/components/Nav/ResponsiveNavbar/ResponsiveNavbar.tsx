import React from "react";

type Props = {};

const ResponsiveNavbar = (props: Props) => {
	return (
		<div className="flex lg:hidden h-[72px] items-center justify-between px-8 fixed w-full">
			ResponsiveNavbar
		</div>
	);
};

export default ResponsiveNavbar;
